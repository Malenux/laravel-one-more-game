export default (() => {

    const customerRegister = document.querySelector('.customer-register');

    customerRegister?.querySelector('form')?.addEventListener('submit', event => {
        event.preventDefault();
    });

    customerRegister?.addEventListener('click', async (event) => {



    });

})();